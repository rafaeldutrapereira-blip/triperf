[build]
builder = "NIXPACKS"
buildCommand = "pip install -r requirements_coach.txt"

[deploy]
startCommand = "python railway_start.py"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[[services]]
name = "labx-api"
